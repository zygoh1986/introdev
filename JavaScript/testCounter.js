/**
 * create a counter that can be incremented by a function
 * or decremented by a function
 * or i can get the curent value of the counter by a function
 * or i can reset the counter to zero
 */




 // Create one function with a loop
/**
 * 1. If the person the increment button, then my counter should be get increment by 1
 */

var count = 0;

 function incrementCount()
 {
     count++;
     alert(count);
 }

 function decrementCount()
 {
     count--;
     alert(count);
 }

 function resetCount()
 {
     count = 0;
     alert(count);
 }

 function currentValue()
 {
    alert(count);
 }

 /** 
 function counter()
 var counter
 {
     if()
     {
         for()
         {

         }

     }
     else if ()
     {
         for ()
         {

         }

     }
 }
 */